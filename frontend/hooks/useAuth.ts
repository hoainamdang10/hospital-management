/**
 * Re-export useAuth from AuthContext for backward compatibility
 * This maintains compatibility with existing code while using the new AuthContext
 */
export { useAuth } from '@/lib/contexts/AuthContext';
