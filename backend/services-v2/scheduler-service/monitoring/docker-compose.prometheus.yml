version: '3.8'

# Docker Compose for Prometheus Monitoring Stack
# Version: 1.0.0
# Last Updated: 2025-10-21

services:
  # ============================================================================
  # PROMETHEUS - Metrics Collection & Alerting
  # ============================================================================
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: scheduler-prometheus
    restart: unless-stopped
    
    # Command line arguments
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-lifecycle'
      - '--web.enable-admin-api'
    
    # Ports
    ports:
      - '9090:9090'
    
    # Volumes
    volumes:
      # Configuration files
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/alerts.yml:/etc/prometheus/alerts.yml:ro
      
      # Data persistence
      - prometheus-data:/prometheus
    
    # Networks
    networks:
      - monitoring
      - scheduler-network
    
    # Health check
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    # Labels
    labels:
      - "com.hospital-management.service=prometheus"
      - "com.hospital-management.component=monitoring"

  # ============================================================================
  # ALERTMANAGER - Alert Routing & Notification (Optional)
  # ============================================================================
  # alertmanager:
  #   image: prom/alertmanager:v0.26.0
  #   container_name: scheduler-alertmanager
  #   restart: unless-stopped
  #   
  #   command:
  #     - '--config.file=/etc/alertmanager/alertmanager.yml'
  #     - '--storage.path=/alertmanager'
  #   
  #   ports:
  #     - '9093:9093'
  #   
  #   volumes:
  #     - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
  #     - alertmanager-data:/alertmanager
  #   
  #   networks:
  #     - monitoring
  #   
  #   healthcheck:
  #     test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:9093/-/healthy"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #   
  #   labels:
  #     - "com.hospital-management.service=alertmanager"
  #     - "com.hospital-management.component=monitoring"

  # ============================================================================
  # NODE EXPORTER - System Metrics (Optional)
  # ============================================================================
  # node-exporter:
  #   image: prom/node-exporter:v1.7.0
  #   container_name: scheduler-node-exporter
  #   restart: unless-stopped
  #   
  #   command:
  #     - '--path.procfs=/host/proc'
  #     - '--path.sysfs=/host/sys'
  #     - '--path.rootfs=/rootfs'
  #     - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
  #   
  #   ports:
  #     - '9100:9100'
  #   
  #   volumes:
  #     - /proc:/host/proc:ro
  #     - /sys:/host/sys:ro
  #     - /:/rootfs:ro
  #   
  #   networks:
  #     - monitoring
  #   
  #   labels:
  #     - "com.hospital-management.service=node-exporter"
  #     - "com.hospital-management.component=monitoring"

  # ============================================================================
  # POSTGRES EXPORTER - Database Metrics (Optional)
  # ============================================================================
  # postgres-exporter:
  #   image: prometheuscommunity/postgres-exporter:v0.15.0
  #   container_name: scheduler-postgres-exporter
  #   restart: unless-stopped
  #   
  #   environment:
  #     DATA_SOURCE_NAME: "postgresql://user:password@postgres:5432/scheduler?sslmode=disable"
  #   
  #   ports:
  #     - '9187:9187'
  #   
  #   networks:
  #     - monitoring
  #     - scheduler-network
  #   
  #   labels:
  #     - "com.hospital-management.service=postgres-exporter"
  #     - "com.hospital-management.component=monitoring"

  # ============================================================================
  # REDIS EXPORTER - Cache Metrics (Optional)
  # ============================================================================
  # redis-exporter:
  #   image: oliver006/redis_exporter:v1.55.0
  #   container_name: scheduler-redis-exporter
  #   restart: unless-stopped
  #   
  #   environment:
  #     REDIS_ADDR: "redis:6379"
  #   
  #   ports:
  #     - '9121:9121'
  #   
  #   networks:
  #     - monitoring
  #     - scheduler-network
  #   
  #   labels:
  #     - "com.hospital-management.service=redis-exporter"
  #     - "com.hospital-management.component=monitoring"

# ============================================================================
# NETWORKS
# ============================================================================
networks:
  monitoring:
    driver: bridge
    name: scheduler-monitoring
  
  scheduler-network:
    external: true
    name: scheduler-network

# ============================================================================
# VOLUMES
# ============================================================================
volumes:
  prometheus-data:
    driver: local
    name: scheduler-prometheus-data
  
  # alertmanager-data:
  #   driver: local
  #   name: scheduler-alertmanager-data

